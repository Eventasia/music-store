apply plugin: 'spring-boot'

dependencies {

    compile project(":user-domain")

    compile "org.springframework.boot:spring-boot-starter-web"
    compile "com.github.eventasia:eventasia-starter:0.0.8.RELEASE"

}